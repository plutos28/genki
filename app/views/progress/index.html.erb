<div class="">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
      <%= notice %>
    </p>
  <% end %>

  <!-- header text -->
  <h1 class="font-bold text-3xl mb-1">Progress</h1>
  <p class="mb-8 text-gray-700">Track your progress towards your ideal body composition.</p>

  <div class="border-b -ml-10 pl-10 pb-0.5 space-x-6">
    <a href="<%= workouts_path %>" class="px-20 text-xl text-purple-500 font-bold border-b border-purple-500 border-b-2">Calories</a>
    <a href="<%= nutrition_index_path %>" class="px-20 text-xl -pb-2 text-gray-500 hover:text-black">Nutrition</a>
  </div>

  <!-- Display TDEE according to GOAL -->
  <div class="mt-10 text-2xl">
    <p class="text-4xl mb-3 capitalize">Current Goal: <%= @current_nutrition_plan.goal %></p>
    <p class="mb-2">Current Total Daily Energy Expenditure (TDEE): <%= @tdee %> calories</p>
    <p>Daily Calories to Meet Goal: <%= @daily_calorie_goal %></p>


  </div>
</div>